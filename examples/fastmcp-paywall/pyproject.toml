[project]
name = "solvapay-fastmcp-paywall-demo"
version = "0.1.0"
description = "FastMCP server gated by SolvaPay paywall — demo for solvapay-python"
requires-python = ">=3.10"
dependencies = [
    "solvapay @ git+https://github.com/dhruv-sanan/solvapay-python@v0.3.0",
    "fastmcp>=0.4",
    "httpx>=0.27",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
