{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"solvapay-python","text":"<p>Community Python SDK for SolvaPay \u2014 agent-native payment rails.</p> <pre><code>pip install solvapay-python\n</code></pre>"},{"location":"#quickstart","title":"Quickstart","text":"<pre><code>from solvapay import SolvaPay\n\nsv = SolvaPay()  # reads SOLVAPAY_SECRET_KEY env var\n\n# Ensure a customer exists\ncustomer_ref = sv.customers.ensure(\"user_123\")\n\n# Create a checkout session\nsession = sv.checkout.create_session(\n    customer_ref=customer_ref,\n    product_ref=\"prd_0QKI8NHF\",\n)\nprint(session.checkout_url)\n</code></pre>"},{"location":"#async","title":"Async","text":"<pre><code>from solvapay import AsyncSolvaPay\n\nasync with AsyncSolvaPay() as sv:\n    session = await sv.checkout.acreate_session(\n        customer_ref=\"cus_123\",\n        product_ref=\"prd_0QKI8NHF\",\n    )\n</code></pre>"},{"location":"#mcp-payable-tools","title":"MCP (payable tools)","text":"<pre><code>from solvapay.paywall import payable_tool\n\n@payable_tool(product=\"prd_0QKI8NHF\")\ndef web_search(*, customer_ref: str, query: str) -&gt; list[str]:\n    ...\n</code></pre>"},{"location":"#api-version-pinning","title":"API version pinning","text":"<pre><code>sv = SolvaPay(api_version=\"2026-05-22\")  # pins Solvapay-Version header\nsv = SolvaPay(api_version=None)          # omits header\n</code></pre>"},{"location":"#install-extras","title":"Install extras","text":"<pre><code>pip install \"solvapay-python[mcp]\"       # FastMCP adapter\npip install \"solvapay-python[langchain]\" # LangChain adapter\npip install \"solvapay-python[retry]\"     # RetryTransport (tenacity)\npip install \"solvapay-python[asgi]\"      # ASGI webhook adapter\n</code></pre>"},{"location":"errors/","title":"Errors","text":"<pre><code>SolvaPayError\n\u251c\u2500\u2500 APIError(status_code, body, request_id, error_code, error_message)\n\u2502   \u251c\u2500\u2500 AuthenticationError    # 401\n\u2502   \u251c\u2500\u2500 PermissionError        # 403\n\u2502   \u251c\u2500\u2500 NotFoundError          # 404\n\u2502   \u251c\u2500\u2500 RateLimitError(retry_after)   # 429\n\u2502   \u251c\u2500\u2500 InvalidRequestError    # other 4xx\n\u2502   \u2514\u2500\u2500 APIServerError         # 5xx\n\u251c\u2500\u2500 APIConnectionError         # network failure\n\u251c\u2500\u2500 APITimeoutError            # timeout\n\u2514\u2500\u2500 PaywallRequired            # paywall gate hit\n    .checkout_url: str | None\n    .checkout_mint_error: APIError | None\n</code></pre>"},{"location":"errors/#handling-errors","title":"Handling errors","text":"<pre><code>from solvapay import SolvaPay\nfrom solvapay.exceptions import (\n    AuthenticationError,\n    RateLimitError,\n    APIConnectionError,\n    PaywallRequired,\n)\n\nsv = SolvaPay()\n\ntry:\n    sv.limits.check(customer_ref=\"cus_1\", product_ref=\"prd_1\")\nexcept AuthenticationError:\n    print(\"Invalid API key\")\nexcept RateLimitError as e:\n    print(f\"Rate limited \u2014 retry after {e.retry_after}s\")\nexcept APIConnectionError:\n    print(\"Network failure \u2014 safe to retry with idempotency key\")\nexcept PaywallRequired as e:\n    print(f\"Paywall hit. Checkout: {e.checkout_url}\")\n</code></pre>"},{"location":"errors/#request_id","title":"<code>request_id</code>","text":"<p>Every <code>APIError</code> captures <code>request_id</code> from <code>x-request-id</code> or <code>x-correlation-id</code> response headers. Include this in support tickets.</p>"},{"location":"errors/#legacy-alias","title":"Legacy alias","text":"<p><code>SolvaPayAPIError</code> is an alias for <code>APIError</code>. It emits <code>DeprecationWarning</code> and will be removed in v2.0. Use <code>APIError</code> directly.</p>"},{"location":"idempotency/","title":"Idempotency","text":"<p>Idempotency keys prevent duplicate operations when a request is retried after a network failure or timeout.</p>"},{"location":"idempotency/#quick-start","title":"Quick start","text":"<pre><code>from solvapay import SolvaPay\nfrom solvapay.idempotency import from_payload\n\nsv = SolvaPay()\n\nkey = from_payload(\"cus_123\", \"prd_abc\", \"checkout\")\nsession = sv.checkout.create_session(\n    customer_ref=\"cus_123\",\n    product_ref=\"prd_abc\",\n    idempotency_key=key,\n)\n</code></pre>"},{"location":"idempotency/#from_payload-key-generation","title":"<code>from_payload</code> \u2014 key generation","text":"<pre><code>from solvapay.idempotency import from_payload\n\nkey = from_payload(*parts, time_bucket=\"day\")\n</code></pre> <p>Produces a 32-hex-character SHA-256 key from the given payload parts.</p>"},{"location":"idempotency/#time_bucket-parameter","title":"<code>time_bucket</code> parameter","text":"Value Behaviour Use when <code>\"day\"</code> (default) Appends current UTC date (<code>2026-05-22</code>). Key changes at midnight UTC. Standard checkout / purchase flows <code>\"hour\"</code> Appends current UTC hour (<code>2026-05-22T14</code>). Key changes every hour. High-frequency operations where 24 h TTL is too wide <code>None</code> Pure payload hash \u2014 deterministic across time. Caller manages TTL externally, or operation has no server-side TTL"},{"location":"idempotency/#retry-contract","title":"Retry contract","text":"<p>Retried POSTs MUST reuse the exact same <code>idempotency_key</code> as the original call.</p> <p>If the key changes between the original request and the retry, the server treats it as a new request, which can cause duplicate charges or records.</p> <pre><code>key = from_payload(\"cus_123\", \"prd_abc\", time_bucket=\"day\")\n\n# Original attempt (may fail with network error)\ntry:\n    session = sv.checkout.create_session(\n        customer_ref=\"cus_123\",\n        product_ref=\"prd_abc\",\n        idempotency_key=key,\n    )\nexcept APIConnectionError:\n    # Retry \u2014 same key, same day \u2192 server deduplicates\n    session = sv.checkout.create_session(\n        customer_ref=\"cus_123\",\n        product_ref=\"prd_abc\",\n        idempotency_key=key,\n    )\n</code></pre>"},{"location":"idempotency/#bucket-roll","title":"Bucket roll","text":"<p>A bucket roll (midnight UTC for <code>\"day\"</code>, hour boundary for <code>\"hour\"</code>) produces a different key. The server will treat a request with the new key as a fresh operation. Do not retry across a bucket boundary unless you intend to create a new operation.</p>"},{"location":"idempotency/#which-operations-accept-idempotency_key","title":"Which operations accept <code>idempotency_key</code>?","text":"<p>All mutating <code>POST</code> operations accept an optional <code>idempotency_key</code> kwarg:</p> <ul> <li><code>sv.checkout.create_session(..., idempotency_key=key)</code></li> <li><code>sv.customers.ensure(..., idempotency_key=key)</code></li> <li><code>sv.usage.track(..., idempotency_key=key)</code></li> <li><code>sv.purchases.cancel(..., idempotency_key=key)</code></li> <li><code>sv.purchases.reactivate(..., idempotency_key=key)</code></li> <li><code>sv.products.create(..., idempotency_key=key)</code></li> <li><code>sv.products.clone(..., idempotency_key=key)</code></li> <li><code>sv.plans.create(..., idempotency_key=key)</code></li> </ul>"},{"location":"idempotency/#server-ttl","title":"Server TTL","text":"<p>The SolvaPay server deduplicates idempotency keys within a TTL window. Keys presented after the TTL expires are treated as new requests. The <code>time_bucket=\"day\"</code> default is conservative and safe for typical retry windows.</p>"},{"location":"migration/","title":"Migration Guide","text":""},{"location":"migration/#v07x-v08x","title":"v0.7.x \u2192 v0.8.x","text":""},{"location":"migration/#flat-methods-are-now-deprecated","title":"Flat methods are now deprecated","text":"<p>All flat methods on <code>SolvaPay</code> / <code>AsyncSolvaPay</code> (e.g. <code>sv.ensure_customer()</code>) now emit <code>DeprecationWarning</code> and will be removed in v2.0.</p> <p>Migrate to the resource namespace API:</p> Old (v0.7) New (v0.8+) <code>sv.ensure_customer(ref)</code> <code>sv.customers.ensure(ref)</code> <code>sv.get_customer(ref)</code> <code>sv.customers.get(ref)</code> <code>sv.check_limits(...)</code> <code>sv.limits.check(...)</code> <code>sv.create_checkout_session(...)</code> <code>sv.checkout.create_session(...)</code> <code>sv.track_usage(...)</code> <code>sv.usage.track(...)</code> <code>sv.cancel_purchase(ref)</code> <code>sv.purchases.cancel(ref)</code> <code>sv.reactivate_purchase(ref)</code> <code>sv.purchases.reactivate(ref)</code> <p>Async variants: prepend <code>a</code> to the method name, e.g. <code>sv.customers.aensure(ref)</code>.</p>"},{"location":"migration/#mock-pattern-changed","title":"Mock pattern changed","text":"<pre><code># Old (v0.7) \u2014 WRONG for v0.8\nclient = MagicMock(spec=SolvaPay)\nclient.check_limits.return_value = ...\n\n# New (v0.8+) \u2014 CORRECT\nclient = MagicMock()\nclient.limits.check.return_value = ...\n</code></pre>"},{"location":"migration/#webhookspy-and-paywallpy-are-now-packages","title":"<code>webhooks.py</code> and <code>paywall.py</code> are now packages","text":"<p>Imports from <code>solvapay.webhooks</code> and <code>solvapay.paywall</code> still work \u2014 the packages re-export everything at the same names.</p>"},{"location":"migration/#v06x-v07x","title":"v0.6.x \u2192 v0.7.x","text":""},{"location":"migration/#wire-format-fixes","title":"Wire format fixes","text":"<ul> <li><code>Customer.customer_ref</code> now accepts <code>reference</code> (real API) and <code>customerRef</code> (legacy) via <code>AliasChoices</code></li> <li><code>BalanceResponse.balance</code> is now major-unit (dollars), not cents</li> <li>Use <code>paywall_state.gate()</code> instead of manual enrichment after <code>check_limits</code></li> </ul>"},{"location":"migration/#solvapayapierror-alias","title":"SolvaPayAPIError alias","text":"<p><code>SolvaPayAPIError = APIError</code> is a deprecated alias. Use <code>APIError</code> directly. Will be removed in v2.0.</p>"},{"location":"troubleshooting/","title":"Troubleshooting","text":"<p>Common errors and how to fix them.</p>"},{"location":"troubleshooting/#1-authenticationerror-api-key-not-set","title":"1. <code>AuthenticationError</code> \u2014 API key not set","text":"<p>Symptom:</p> <pre><code>solvapay.exceptions.AuthenticationError: No API key provided.\n</code></pre> <p>Cause: <code>SOLVAPAY_SECRET_KEY</code> env var is not set and no <code>api_key=</code> argument was passed.</p> <p>Fix:</p> <pre><code>export SOLVAPAY_SECRET_KEY=sk_sandbox_...\n</code></pre> <p>Or pass it explicitly:</p> <pre><code>sv = SolvaPay(api_key=\"sk_sandbox_...\")\n</code></pre>"},{"location":"troubleshooting/#2-async-in-sync-deadlock","title":"2. Async-in-sync deadlock","text":"<p>Symptom:</p> <pre><code>RuntimeError: cannot run event loop while another event loop is running\n</code></pre> <p>Or a call to <code>await sv.customers.ensure(...)</code> hangs indefinitely.</p> <p>Cause: Mixing <code>AsyncSolvaPay</code> with sync framework code, or running async SDK methods from a Jupyter notebook without <code>await</code>.</p> <p>Fix: Use <code>SolvaPay</code> (sync) for sync frameworks (FastAPI route handlers are async \u2014 use <code>AsyncSolvaPay</code> there). In Jupyter, <code>await</code> the call directly or use <code>asyncio.run()</code> in a standalone script.</p> <pre><code># FastAPI \u2014 use AsyncSolvaPay\n@app.get(\"/check\")\nasync def check(customer_ref: str):\n    async with AsyncSolvaPay() as sv:\n        result = await sv.limits.acheck(customer_ref=customer_ref, product_ref=\"prd_x\")\n    return result\n</code></pre>"},{"location":"troubleshooting/#3-modulenotfounderror-missing-optional-extra","title":"3. <code>ModuleNotFoundError</code> \u2014 missing optional extra","text":"<p>Symptom:</p> <pre><code>ModuleNotFoundError: No module named 'fastmcp'\nModuleNotFoundError: No module named 'langchain_core'\nModuleNotFoundError: No module named 'tenacity'\n</code></pre> <p>Cause: Optional adapter or middleware not installed.</p> <p>Fix: Install the relevant extra:</p> <pre><code>pip install solvapay-python[mcp]       # fastmcp\npip install solvapay-python[langchain] # langchain-core\npip install solvapay-python[retry]     # tenacity (RetryTransport)\npip install solvapay-python[fastapi]   # fastapi\npip install solvapay-python[bench]     # pytest-benchmark (dev only)\n</code></pre>"},{"location":"troubleshooting/#4-contract-tests-skip-silently","title":"4. Contract tests skip silently","text":"<p>Symptom:</p> <pre><code>3 skipped \u2014 SOLVAPAY_SANDBOX_KEY not set\n</code></pre> <p>Cause: Sandbox tests require a live sandbox key and are intentionally skipped in CI unless the secret is configured.</p> <p>Fix: For local sandbox testing:</p> <pre><code>export SOLVAPAY_SANDBOX_KEY=sk_sandbox_...\nexport SOLVAPAY_TEST_CUSTOMER_REF=cus_...\nexport SOLVAPAY_TEST_PRODUCT_REF=prd_...\nuv run pytest -m contract\n</code></pre>"},{"location":"troubleshooting/#5-windows-virtualenv-uv-path-issues","title":"5. Windows virtualenv / <code>uv</code> path issues","text":"<p>Symptom:</p> <pre><code>FileNotFoundError: [WinError 2] The system cannot find the file specified\n</code></pre> <p>Or <code>uv run</code> fails to find the installed package.</p> <p>Cause: Windows uses different path separators and virtualenv activation conventions.</p> <p>Fix: Use <code>uv</code> from the project root \u2014 it manages the <code>.venv</code> automatically:</p> <pre><code>cd path\\to\\solvapay-python\nuv sync --all-extras --dev\nuv run pytest\n</code></pre> <p>Do not manually activate the venv; let <code>uv run</code> handle it.</p>"},{"location":"troubleshooting/#6-deprecationwarning-flat-method-deprecated-in-test-output","title":"6. <code>DeprecationWarning: Flat method deprecated</code> in test output","text":"<p>Symptom:</p> <pre><code>DeprecationWarning: Flat method deprecated. Use sv.customers.ensure(...) instead.\n</code></pre> <p>Cause: Old flat-method API (e.g. <code>sv.ensure_customer(...)</code>) is still being called. These shims emit a <code>DeprecationWarning</code> on every call and will be removed in v2.0.</p> <p>Fix: Migrate to the namespace API:</p> <pre><code># Old (deprecated)\nsv.ensure_customer(\"ext_ref\")\n\n# New (stable)\nsv.customers.ensure(\"ext_ref\")\n</code></pre> <p>To silence the warning in tests that still use the old path intentionally, add to <code>pyproject.toml</code>:</p> <pre><code>filterwarnings = [\"ignore:Flat method deprecated:DeprecationWarning\"]\n</code></pre>"},{"location":"troubleshooting/#7-paywallrequired-raised-with-checkout_urlnone","title":"7. <code>PaywallRequired</code> raised with <code>checkout_url=None</code>","text":"<p>Symptom:</p> <pre><code>except PaywallRequired as e:\n    print(e.checkout_url)  # None \u2014 no redirect URL available\n</code></pre> <p>Cause: In v0.8/v0.9, the SDK does NOT automatically call <code>checkout.create_session()</code> on every gate hit. Auto-minting is triggered only when the <code>LimitResponse</code> from the server includes a <code>checkoutUrl</code> field.</p> <p>Fix: Use <code>paywall_state.gate()</code> for a fully enriched decision, or construct the checkout URL yourself:</p> <pre><code>from solvapay.paywall_state import gate\n\ndecision = gate(sv, customer_ref=ref, product_ref=\"prd_x\")\nif decision.state != PaywallState.OK:\n    redirect_to(decision.checkout_url)\n</code></pre> <p>Note: in v1.0 this behavior changes \u2014 see <code>HLD.md \u00a7V1.6 PW4</code>.</p>"},{"location":"troubleshooting/#8-verify_webhook-raises-valueerror","title":"8. <code>verify_webhook</code> raises <code>ValueError</code>","text":"<p>Symptom:</p> <pre><code>ValueError: No valid signature found\nValueError: Request timestamp too old\n</code></pre> <p>Cause: - Wrong secret (key rotation in progress, or <code>SOLVAPAY_WEBHOOK_SECRET</code> not matching the SolvaPay dashboard secret). - System clock drift exceeding <code>tolerance</code> (default 300 seconds).</p> <p>Fix:</p> <pre><code># Check your secret matches the SolvaPay dashboard exactly.\nverify_webhook(body=raw_body, signature=header, secret=os.environ[\"SOLVAPAY_WEBHOOK_SECRET\"])\n\n# For testing with sign_webhook, pass a recent timestamp:\nfrom solvapay.webhooks import sign_webhook\nsig = sign_webhook(body, secret, timestamp=int(time.time()))\n</code></pre>"},{"location":"troubleshooting/#9-mypy-strict-errors-in-paywall-tests","title":"9. <code>mypy --strict</code> errors in paywall tests","text":"<p>Symptom:</p> <pre><code>error: Argument \"client\" to \"Paywall\" has incompatible type \"MagicMock\"; expected \"SolvaPay\"\n</code></pre> <p>Cause: <code>MagicMock(spec=SolvaPay)</code> does not know about instance attrs like <code>.limits</code>, <code>.customers</code> set in <code>__init__</code>. The spec reflects the class, not the instance.</p> <p>Fix: Use plain <code>MagicMock()</code> without <code>spec=</code>:</p> <pre><code># WRONG\nclient = MagicMock(spec=SolvaPay)  # AttributeError on .limits\n\n# CORRECT\nclient = MagicMock()\nclient.limits.check.return_value = LimitResponse(within_limits=True, remaining=5)\n</code></pre> <p>Add <code># type: ignore[arg-type]</code> on the <code>Paywall(client=...)</code> line to satisfy mypy strict.</p>"},{"location":"troubleshooting/#10-pip-install-solvapay-vs-pip-install-solvapay-python","title":"10. <code>pip install solvapay</code> vs <code>pip install solvapay-python</code>","text":"<p>Symptom:</p> <pre><code>ERROR: No matching distribution found for solvapay\n</code></pre> <p>Or: installed <code>solvapay</code> but <code>from solvapay import SolvaPay</code> is empty / old.</p> <p>Cause: The distribution is named <code>solvapay-python</code> on PyPI (avoids namespace conflict before official adoption). The import name is still <code>solvapay</code>.</p> <p>Fix:</p> <pre><code>pip install solvapay-python          # correct\npip install solvapay-python[mcp]     # with MCP adapter\n</code></pre> <p>The import surface is unchanged:</p> <pre><code>from solvapay import SolvaPay, AsyncSolvaPay\n</code></pre>"},{"location":"webhooks/","title":"Webhooks","text":""},{"location":"webhooks/#verify-a-webhook","title":"Verify a webhook","text":"<pre><code>from solvapay.webhooks import verify_webhook\n\nevent = verify_webhook(\n    body=request.body,      # raw request body string\n    signature=request.headers[\"sv-signature\"],\n    secret=\"whsec_...\",\n    tolerance=300,          # max clock skew in seconds\n)\nprint(event[\"type\"])  # e.g. \"payment.succeeded\"\n</code></pre>"},{"location":"webhooks/#webhookpipeline-verify-dedup","title":"WebhookPipeline (verify + dedup)","text":"<pre><code>from solvapay.webhooks import WebhookPipeline\n\npipeline = WebhookPipeline(\n    secrets=[\"whsec_primary\", \"whsec_rotating\"],\n    max_clock_skew_seconds=300,   # two separate knobs\n    replay_ttl_seconds=600,\n)\n\nenvelope = pipeline.process(body=raw_bytes, signature=sig_header)\nprint(envelope.event_id)\nprint(envelope.event[\"type\"])\n</code></pre>"},{"location":"webhooks/#secret-rotation","title":"Secret rotation","text":"<p>Pass multiple secrets \u2014 primary first, fallback after:</p> <pre><code>pipeline = WebhookPipeline(secrets=[\"whsec_new\", \"whsec_old\"])\n</code></pre> <p>The pipeline tries the primary HMAC; on mismatch (not age failure), tries the secondary.</p>"},{"location":"webhooks/#replay-protection","title":"Replay protection","text":"<p><code>InMemorySeenEventCache</code> deduplicates by <code>event_id</code> for <code>replay_ttl_seconds</code>. Replace with a Redis-backed cache for multi-process deployments:</p> <pre><code>from solvapay.webhooks import WebhookPipeline, SeenEventCache\n\nclass RedisCache:\n    def try_claim(self, event_id: str, ttl_seconds: int) -&gt; bool:\n        return redis.set(f\"webhook:{event_id}\", 1, ex=ttl_seconds, nx=True)\n\npipeline = WebhookPipeline(secrets=[\"whsec_...\"], seen_cache=RedisCache())\n</code></pre>"},{"location":"webhooks/#asgi-adapter","title":"ASGI adapter","text":"<pre><code>from solvapay.adapters.asgi import webhook_app\nfrom solvapay.webhooks import WebhookPipeline\n\npipeline = WebhookPipeline(secrets=[\"whsec_...\"])\n\nasync def handle(envelope):\n    print(envelope.event[\"type\"])\n\napp = webhook_app(pipeline, handle, path=\"/webhook\")\n# Mount on FastAPI: app.mount(\"/webhook\", asgi_app)\n</code></pre>"},{"location":"webhooks/#signing-webhooks-for-tests","title":"Signing webhooks (for tests)","text":"<pre><code>from solvapay.webhooks import sign_webhook\n\nsig = sign_webhook(b'{\"id\":\"evt_1\"}', secret=\"whsec_test\")\n# \u2192 \"t=1234567890,v1=abc123...\"\n</code></pre>"},{"location":"architecture/cold-start/","title":"Cold-Start Performance","text":""},{"location":"architecture/cold-start/#what-we-measure","title":"What we measure","text":"<p><code>python -X importtime -c \"import solvapay\"</code> prints a line per imported module:</p> <pre><code>import time:    &lt;self_\u00b5s&gt; | &lt;cumulative_\u00b5s&gt; | &lt;module_name&gt;\n</code></pre> <p>The <code>cumulative_\u00b5s</code> column for the root <code>solvapay</code> module is the total cold-import cost \u2014 the number we track.</p>"},{"location":"architecture/cold-start/#the-baseline-harness-teststest_cold_importpy","title":"The baseline harness (<code>tests/test_cold_import.py</code>)","text":"<p>Introduced in v0.9.2, <code>tests/test_cold_import.py</code> runs the above command via <code>subprocess</code> and:</p> <ol> <li>First run \u2014 writes <code>tests/_baselines/cold_import.json</code> with the measured value.</li> <li>Subsequent runs \u2014 asserts the current measurement is within 1.5\u00d7 of the baseline.</li> </ol> <p>The 1.5\u00d7 regression factor is intentionally loose \u2014 it catches gross regressions (importing a new heavy dependency eagerly) without failing on normal M-series vs CI machine variance.</p> <p>The committed baseline is re-measured and updated whenever a structural change affects import cost (e.g. lazy adapter imports in v0.9.2).</p>"},{"location":"architecture/cold-start/#current-baseline","title":"Current baseline","text":"Version M-series (macOS) What changed v0.9.1 ~88 ms Initial measurement v0.9.2 ~100 ms PEP 562 <code>__getattr__</code> + <code>__dir__</code> added; adapters lazy"},{"location":"architecture/cold-start/#pep-562-lazy-adapter-imports","title":"PEP 562 lazy adapter imports","text":"<p>Adapter modules (<code>solvapay.adapters.mcp</code>, <code>solvapay.adapters.langchain</code>, etc.) depend on optional heavy packages \u2014 <code>fastmcp</code>, <code>langchain-core</code>, <code>fastapi</code>. These are only installed when the user runs <code>pip install solvapay-python[mcp]</code> etc.</p> <p>Before v0.9.2, accessing <code>solvapay.adapters</code> required an explicit subpackage import. In v0.9.2, <code>solvapay/__init__.py</code> defines a PEP 562 <code>__getattr__</code> that loads <code>solvapay.adapters</code> lazily on first attribute access:</p> <pre><code># In solvapay/__init__.py\n_LAZY_MODULES = {\"adapters\": \"solvapay.adapters\"}\n\ndef __getattr__(name: str) -&gt; object:\n    if name in _LAZY_MODULES:\n        import importlib\n        mod = importlib.import_module(_LAZY_MODULES[name])\n        globals()[name] = mod  # cache for subsequent accesses\n        return mod\n    raise AttributeError(f\"module 'solvapay' has no attribute {name!r}\")\n\ndef __dir__() -&gt; list[str]:\n    return sorted(set(globals()) | set(_LAZY_MODULES))\n</code></pre> <p>This ensures: - <code>import solvapay</code> \u2014 zero adapter cost regardless of which extras are installed. - <code>solvapay.adapters</code> \u2014 triggers a single load on first access, cached thereafter. - <code>from solvapay.adapters import mcp</code> \u2014 works via Python's normal subpackage import path (unaffected by <code>__getattr__</code>). - <code>dir(solvapay)</code> \u2014 lists <code>\"adapters\"</code> per PEP 562 convention.</p>"},{"location":"architecture/cold-start/#v10-hard-budget-hld-v120","title":"v1.0 hard budget (HLD \u00a7V1.20)","text":"<p>v0.9.2 ships measurement only. The hard gate is a v1.0 feature:</p> Environment Budget M-series Mac &lt; 150 ms x86 CI &lt; 300 ms <p>v1.0 achieves this primarily via <code>@cached_property</code> lazy namespace materialization \u2014 <code>SolvaPay.__init__</code> will no longer eagerly construct <code>customers</code>, <code>checkout</code>, <code>limits</code>, etc. Each namespace is constructed on first access (<code>sv.customers.ensure(...)</code>) and cached as an instance attribute. This eliminates the linear startup cost as V2/V3 expand the namespace count.</p> <p>See <code>HLD.md \u00a7V1.3 RN1-v2</code> and <code>\u00a7V1.20</code> for the full contract.</p>"},{"location":"architecture/layers/","title":"Layer Architecture (HLD V1.1)","text":"<p>The SDK is organized into strict layers. Each layer may only import from layers below it. Violations fail CI via <code>import-linter</code> (see <code>tools/importlinter.cfg</code>).</p> <p>From v0.9.3 the DAG is enforced as a single <code>type = layers</code> contract, replacing the earlier three sparse <code>forbidden</code> contracts. <code>exhaustive = true</code> flags any new top-level module that ships without an explicit layer assignment.</p>"},{"location":"architecture/layers/#layer-dag","title":"Layer DAG","text":"<pre><code>L7  solvapay.fastapi           \u2014 Top-level adapter shims (re-export from L6.adapters.*)\n    solvapay.langchain\nL6  solvapay.adapters          \u2014 Framework adapters (MCP, LangChain, ASGI, FastAPI)\nL5  solvapay.paywall           \u2014 Paywall / AsyncPaywall primitives\n    solvapay.webhooks          \u2014 Webhook verify + pipeline\nL4  solvapay.client            \u2014 SolvaPay / AsyncSolvaPay public facades\n    solvapay._async_client\nL3  solvapay.operations        \u2014 Resource namespace operations (sv.customers.ensure etc.)\nL2  solvapay.models            \u2014 Pydantic request/response models\nL1  solvapay._http             \u2014 HTTP-client wrapper around the transport kernel\nL0  solvapay._transport        \u2014 Transport Protocol + httpx impl + middleware\n</code></pre> <p>Note. The HLD ships an aspirational L1 <code>_wire</code> slot that is currently unused. The shipped layout uses L1 for <code>_http</code> (the existing transport-kernel wrapper). When <code>_wire</code> ships, it will sit between <code>_transport</code> and <code>_http</code>.</p>"},{"location":"architecture/layers/#allowed-imports","title":"Allowed imports","text":"<p>A module at L_n may import from any module at L_(n-1) or below, plus the layer-independent leaf modules (<code>exceptions</code>, <code>_stability</code>, <code>_config</code>, <code>idempotency</code>, <code>events</code>, <code>paywall_state</code>).</p> Layer May import L0 <code>_transport</code> stdlib, httpx, leaf modules L1 <code>_http</code> L0 + leaf modules L2 <code>models</code> stdlib, pydantic, leaf modules L3 <code>operations</code> L0\u2013L2 + leaf modules L4 <code>client</code> / <code>_async_client</code> L0\u2013L3 + leaf modules L5 <code>paywall</code> / <code>webhooks</code> L0\u2013L4 + leaf modules L6 <code>adapters</code> L0\u2013L5 + leaf modules L7 <code>fastapi</code> / <code>langchain</code> (top-level shims) L0\u2013L6 + leaf modules <code>tests/</code> everything (permissive; tests live outside the <code>solvapay</code> container) <p>Sibling modules on the same row (e.g. <code>paywall</code> \u2194 <code>webhooks</code>, or <code>client</code> \u2194 <code>_async_client</code>) may not import each other. Each is independently constrained against the layers below.</p>"},{"location":"architecture/layers/#why-l7-stability-tier-not-function-dg2-lock","title":"Why L7 = stability tier, not function (DG2 lock)","text":"<p><code>solvapay.experimental</code> (reserved for v1.0) groups features by stability, not by domain. A webhook adapter that is still unstable lives in L6 (adapters) while it's being proven, then graduates to the canonical path once stable. This prevents the <code>experimental</code> namespace from becoming a permanent home for half-finished features.</p>"},{"location":"architecture/layers/#leaf-modules-layer-independent","title":"Leaf modules (layer-independent)","text":"<p>These are listed in <code>exhaustive_ignores</code> in the contract because they have no upward edges and no downward consumers beyond the boundary primitives:</p> <ul> <li><code>solvapay.exceptions</code></li> <li><code>solvapay._stability</code></li> <li><code>solvapay._config</code></li> <li><code>solvapay.idempotency</code></li> <li><code>solvapay.events</code></li> <li><code>solvapay.paywall_state</code></li> </ul>"},{"location":"architecture/layers/#type_checking-imports","title":"TYPE_CHECKING imports","text":"<p>The contract sets <code>ignore_type_checking_imports = true</code>. Imports inside <code>if TYPE_CHECKING:</code> blocks are stripped from the dependency graph, so cross-layer type hints (e.g. <code>from solvapay.client import SolvaPay</code> for annotation purposes) do not trip the gate. <code>mypy</code> still resolves these via the standard <code>from __future__ import annotations</code> pattern enforced by <code>tools/lint_invariants.py</code>.</p>"},{"location":"architecture/layers/#enforcement","title":"Enforcement","text":"<pre><code>~/.local/bin/uv run lint-imports --config tools/importlinter.cfg\n</code></pre> <p>Runs in CI on every push and PR. Also wrapped by <code>tests/test_layer_dag.py</code> so the contract is exercised by <code>pytest</code> for local feedback.</p>"},{"location":"guides/fastapi/","title":"FastAPI Integration","text":""},{"location":"guides/fastapi/#webhook-router","title":"Webhook router","text":"<pre><code>from fastapi import FastAPI\nfrom solvapay.fastapi import webhook_router\n\napp = FastAPI()\napp.include_router(\n    webhook_router(\n        secret=\"whsec_...\",\n        on_event=handle_event,\n        path=\"/webhook\",\n    )\n)\n</code></pre>"},{"location":"guides/fastapi/#asgi-webhook-app-framework-agnostic","title":"ASGI webhook app (framework-agnostic)","text":"<pre><code>from fastapi import FastAPI\nfrom solvapay.adapters.asgi import webhook_app\nfrom solvapay.webhooks import WebhookPipeline\n\npipeline = WebhookPipeline(secrets=[\"whsec_...\"])\n\nasync def handle(envelope):\n    event = envelope.event\n    print(f\"Received {event['type']} for {event.get('customerId')}\")\n\napp = FastAPI()\napp.mount(\"/webhook\", webhook_app(pipeline, handle))\n</code></pre>"},{"location":"guides/fastapi/#paywall-decorator","title":"Paywall decorator","text":"<pre><code>from fastapi import FastAPI, Request\nfrom solvapay.paywall import require\nfrom solvapay.exceptions import PaywallRequired\n\napp = FastAPI()\n\n@app.post(\"/api/search\")\n@require(product=\"prd_0QKI8NHF\", plan=\"pln_pro\", customer_ref_arg=\"customer_id\")\nasync def search(customer_id: str, query: str):\n    return {\"results\": [...]}\n\n@app.exception_handler(PaywallRequired)\nasync def paywall_handler(req: Request, exc: PaywallRequired):\n    return {\"error\": \"upgrade_required\", \"checkout_url\": exc.checkout_url}\n</code></pre>"},{"location":"guides/langchain/","title":"LangChain Integration","text":"<p>Monetize any LangChain tool with SolvaPay.</p>"},{"location":"guides/langchain/#install","title":"Install","text":"<pre><code>pip install \"solvapay-python[langchain]\"\n</code></pre>"},{"location":"guides/langchain/#monetize-a-tool","title":"Monetize a tool","text":"<pre><code>from solvapay import SolvaPay\nfrom solvapay.adapters.langchain import monetize_tool\n\nsv = SolvaPay()\n\n# Any LangChain BaseTool-compatible object\nmonetized = monetize_tool(\n    my_langchain_tool,\n    product=\"prd_0QKI8NHF\",\n    customer_ref_arg=\"customer_ref\",\n    client=sv,\n)\n</code></pre>"},{"location":"guides/langchain/#with-payable_tool","title":"With <code>@payable_tool</code>","text":"<p>If a tool is already decorated with <code>@payable_tool</code>, <code>monetize_tool</code> reads <code>__solvapay_meta__</code> automatically:</p> <pre><code>from solvapay.paywall import payable_tool\nfrom solvapay.adapters.langchain import monetize_tool\n\n@payable_tool(product=\"prd_0QKI8NHF\")\ndef web_search(*, customer_ref: str, query: str) -&gt; list[str]: ...\n\nmonetized = monetize_tool(web_search, client=sv)\n</code></pre>"},{"location":"guides/langchain/#see-also","title":"See also","text":"<ul> <li><code>examples/langchain-paywall/</code> \u2014 standalone LangChain agent example</li> <li><code>examples/multi-framework-paywall/</code> \u2014 same tool in MCP + LangChain + async</li> </ul>"},{"location":"guides/mcp/","title":"MCP Integration","text":"<p>Monetize any MCP tool with SolvaPay using <code>@payable_tool</code>.</p>"},{"location":"guides/mcp/#install","title":"Install","text":"<pre><code>pip install \"solvapay-python[mcp]\"\n</code></pre>"},{"location":"guides/mcp/#stamp-a-tool","title":"Stamp a tool","text":"<pre><code>from solvapay.paywall import payable_tool\n\n@payable_tool(product=\"prd_0QKI8NHF\")\ndef web_search(*, customer_ref: str, query: str) -&gt; list[str]:\n    \"\"\"Search the web.\"\"\"\n    return [...]\n</code></pre>"},{"location":"guides/mcp/#register-with-fastmcp","title":"Register with FastMCP","text":"<pre><code>from fastmcp import FastMCP\nfrom solvapay.adapters.mcp import register_payable_tool_fastmcp\n\nmcp = FastMCP(\"my-server\")\nregister_payable_tool_fastmcp(mcp, web_search)\n</code></pre>"},{"location":"guides/mcp/#schema-flavors","title":"Schema flavors","text":"<pre><code>from solvapay.adapters.mcp import (\n    payable_tool_mcp_schema,\n    payable_tool_anthropic_tool,\n    payable_tool_openai_function,\n    payable_tool_langchain_args_schema,\n)\n\nmcp_schema = payable_tool_mcp_schema(web_search)\nanthropic_tool = payable_tool_anthropic_tool(web_search)\nopenai_fn = payable_tool_openai_function(web_search)\nlangchain_schema = payable_tool_langchain_args_schema(web_search)\n</code></pre>"},{"location":"guides/mcp/#multi-framework-example","title":"Multi-framework example","text":"<p>See <code>examples/multi-framework-paywall/</code> for a complete example running one tool across FastMCP, LangChain, and raw async.</p>"},{"location":"reference/client/","title":"Client Reference","text":"<p>Synchronous SolvaPay API client.</p> <p>Resource namespaces (v0.8+):     sv.customers.ensure / get / update / balance     sv.checkout.create_session     sv.limits.check     sv.purchases.cancel / reactivate     sv.usage.track     sv.products.list / get / create / delete / clone     sv.plans.list / create / update / delete     sv.merchant.get / get_platform_config</p> <p>Flat methods (deprecated, removed in 2.0):     sv.ensure_customer, sv.create_checkout_session, etc.</p> <p>Parameters:</p> Name Type Description Default <code>api_key</code> <code>str | None</code> <p>SolvaPay secret key. Falls back to SOLVAPAY_SECRET_KEY env var.</p> <code>None</code> <code>base_url</code> <code>str | None</code> <p>API base URL. Falls back to SOLVAPAY_API_BASE_URL env var,       then to https://api.solvapay.com.</p> <code>None</code> <code>timeout</code> <code>float</code> <p>HTTP timeout in seconds. Default 30.</p> <code>30.0</code> Example <p>from solvapay import SolvaPay sv = SolvaPay() customer_ref = sv.customers.ensure(\"user_123\") session = sv.checkout.create_session( ...     customer_ref=customer_ref, product_ref=\"prd_0QKI8NHF\" ... ) print(session.checkout_url)</p> <p>Async SolvaPay API client.</p> <p>Resource namespaces (v0.8+):     sv.customers.aensure / aget / aupdate / abalance     sv.checkout.acreate_session     sv.limits.acheck     sv.purchases.acancel / areactivate     sv.usage.atrack     sv.products.alist / aget / acreate / adelete / aclone     sv.plans.alist / acreate / aupdate / adelete     sv.merchant.aget / aget_platform_config</p> <p>Parameters:</p> Name Type Description Default <code>api_key</code> <code>str | None</code> <p>SolvaPay secret key. Falls back to SOLVAPAY_SECRET_KEY env var.</p> <code>None</code> <code>base_url</code> <code>str | None</code> <p>API base URL. Falls back to SOLVAPAY_API_BASE_URL env var,       then to https://api.solvapay.com.</p> <code>None</code> <code>timeout</code> <code>float</code> <p>HTTP timeout in seconds. Default 30.</p> <code>30.0</code> Example <p>async with AsyncSolvaPay() as sv: ...     session = await sv.checkout.acreate_session( ...         customer_ref=\"cus_123\", product_ref=\"prd_0QKI8NHF\" ...     )</p>"},{"location":"reference/exceptions/","title":"Exceptions Reference","text":"<p>Exception hierarchy for SolvaPay SDK.</p>"},{"location":"reference/exceptions/#solvapay.exceptions.APIConnectionError","title":"<code>APIConnectionError</code>","text":"<p>               Bases: <code>SolvaPayError</code></p> <p>Network-level error \u2014 connection refused, reset, or DNS failure.</p>"},{"location":"reference/exceptions/#solvapay.exceptions.APIError","title":"<code>APIError</code>","text":"<p>               Bases: <code>SolvaPayError</code></p> <p>Raised when the SolvaPay API returns a non-2xx response.</p>"},{"location":"reference/exceptions/#solvapay.exceptions.APIServerError","title":"<code>APIServerError</code>","text":"<p>               Bases: <code>APIError</code></p> <p>5xx \u2014 server-side error.</p>"},{"location":"reference/exceptions/#solvapay.exceptions.APITimeoutError","title":"<code>APITimeoutError</code>","text":"<p>               Bases: <code>SolvaPayError</code></p> <p>Request timed out before the server responded.</p>"},{"location":"reference/exceptions/#solvapay.exceptions.AuthenticationError","title":"<code>AuthenticationError</code>","text":"<p>               Bases: <code>APIError</code></p> <p>401 \u2014 invalid or missing API key.</p>"},{"location":"reference/exceptions/#solvapay.exceptions.InvalidRequestError","title":"<code>InvalidRequestError</code>","text":"<p>               Bases: <code>APIError</code></p> <p>4xx (non-401/403/404/429) \u2014 malformed or invalid request.</p>"},{"location":"reference/exceptions/#solvapay.exceptions.NotFoundError","title":"<code>NotFoundError</code>","text":"<p>               Bases: <code>APIError</code></p> <p>404 \u2014 requested resource does not exist.</p>"},{"location":"reference/exceptions/#solvapay.exceptions.PermissionError","title":"<code>PermissionError</code>","text":"<p>               Bases: <code>APIError</code></p> <p>403 \u2014 insufficient permissions for this operation.</p>"},{"location":"reference/exceptions/#solvapay.exceptions.RateLimitError","title":"<code>RateLimitError</code>","text":"<p>               Bases: <code>APIError</code></p> <p>429 \u2014 rate limit exceeded. Check .retry_after for the suggested delay (seconds).</p>"},{"location":"reference/exceptions/#solvapay.exceptions.SolvaPayError","title":"<code>SolvaPayError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Base exception for all SolvaPay SDK errors.</p>"},{"location":"reference/models/","title":"Models Reference","text":"<p>Pydantic models for SolvaPay API request/response payloads.</p> <p>Python fields are snake_case. API wire format is camelCase. Field(alias=\"camelCase\") handles the mapping; populate_by_name=True lets callers use either form during construction.</p>"},{"location":"reference/models/#solvapay.models.BalanceResponse","title":"<code>BalanceResponse</code>","text":"<p>               Bases: <code>_Base</code></p> <p>Customer credit balance from GET /v1/sdk/customers/{ref}/balance.</p> <p>Real API returns credits as an integer in minor units along with the conversion factor (e.g., credits=100000, creditsPerMinorUnit=100 means 1000 display units). <code>balance</code> and <code>currency</code> are derived for backward compatibility with the pre-v0.7.0 shape.</p>"},{"location":"reference/models/#solvapay.models.CheckoutSession","title":"<code>CheckoutSession</code>","text":"<p>               Bases: <code>_Base</code></p> <p>Response from POST /v1/sdk/checkout-sessions.</p>"},{"location":"reference/models/#solvapay.models.Merchant","title":"<code>Merchant</code>","text":"<p>               Bases: <code>_Base</code></p> <p>Merchant account details.</p>"},{"location":"reference/models/#solvapay.models.Plan","title":"<code>Plan</code>","text":"<p>               Bases: <code>_Base</code></p> <p>A SolvaPay plan attached to a product.</p>"},{"location":"reference/models/#solvapay.models.PlatformConfig","title":"<code>PlatformConfig</code>","text":"<p>               Bases: <code>_Base</code></p> <p>Platform-level configuration.</p>"},{"location":"reference/models/#solvapay.models.Product","title":"<code>Product</code>","text":"<p>               Bases: <code>_Base</code></p> <p>A SolvaPay product.</p>"},{"location":"reference/paywall/","title":"Paywall Reference","text":"<p>Synchronous paywall gate. Requires a SolvaPay (sync) client (HLD V1.6 PW1).</p> <p>Async paywall gate. Requires an AsyncSolvaPay client (HLD V1.6 PW1).</p> <p>               Bases: <code>SolvaPayError</code></p> <p>Raised when a customer has exceeded their limits.</p> <p>checkout_mint_error is set when the paywall tried to mint a checkout URL automatically but the create_checkout_session call failed (HLD V1.6 PW3).</p> <p>Stamped as fn.solvapay_meta by @payable_tool.</p> <p>_meta_version is locked at 1 (HLD AD1). Never break this shape.</p>"},{"location":"reference/paywall/#solvapay.paywall.core.Paywall.gate","title":"<code>gate(*args, **kwargs)</code>","text":"<p>Check limits. Raises PaywallRequired if blocked, returns None if OK.</p>"},{"location":"reference/paywall/#solvapay.paywall.core.AsyncPaywall.gate","title":"<code>gate(*args, **kwargs)</code>  <code>async</code>","text":"<p>Async limit check. Raises PaywallRequired if blocked.</p>"},{"location":"reference/webhooks/","title":"Webhooks Reference","text":"<p>Verify a SolvaPay webhook signature and return the parsed event.</p> <p>Verify signature, check clock skew, deduplicate (HLD V1.7 WP2).</p> <p>TWO separate knobs (HLD WP2 lock):     max_clock_skew_seconds  \u2014 clock tolerance (default 300s)     replay_ttl_seconds      \u2014 replay-attack dedup window (default 600s)</p> <p>Parsed and verified webhook event (HLD V1.7).</p> <p>Try primary secret; fall back to secondary on HMAC mismatch (not age failure).</p> <p>Both comparisons are constant-time (hmac.compare_digest). Age check happens upstream in WebhookPipeline.process \u2014 this class only handles HMAC matching. Pass secrets in order of preference: primary first, rotation candidates after.</p> <p>Sync in-memory cache. Thread-safe via threading.Lock (HLD WP1 atomic).</p> <p>Async in-memory cache. asyncio.Lock for async pipelines (HLD WP1 atomic).</p> <p>Produce a <code>sv-signature</code> header value for a webhook payload.</p> <p>Returns a string in the format <code>t={ts},v1={hmac}</code> \u2014 the same format verified by :func:<code>solvapay.webhooks.verify.verify_webhook</code> and :class:<code>solvapay.webhooks.pipeline.WebhookPipeline</code>.</p> <p>Parameters:</p> Name Type Description Default <code>body</code> <code>bytes</code> <p>Raw request body bytes.</p> required <code>secret</code> <code>str</code> <p>Webhook secret (<code>whsec_...</code>).</p> required <code>timestamp</code> <code>int | None</code> <p>Unix timestamp. Defaults to current time.</p> <code>None</code> <p>Example::</p> <pre><code>from solvapay.webhooks import sign_webhook, verify_webhook\n\nsig = sign_webhook(b'{\"id\":\"evt_1\"}', secret=\"whsec_test\")\nverify_webhook(b'{\"id\":\"evt_1\"}', sig, secret=\"whsec_test\")  # passes\n</code></pre>"},{"location":"reference/webhooks/#solvapay.webhooks.pipeline.WebhookPipeline.process","title":"<code>process(body, signature)</code>","text":"<p>Verify signature, check age, deduplicate, return envelope.</p> <p>Raises SolvaPayError on any verification failure.</p>"},{"location":"reference/webhooks/#solvapay.webhooks.rotation.MultiSecretVerifier.verify","title":"<code>verify(*, payload, received)</code>","text":"<p>Return True if the primary secret matches, or any fallback secret matches.</p> <p>Short-circuits on first match. Each comparison is constant-time.</p>"},{"location":"rfcs/0001-spending-policy/","title":"RFC 0001 \u2014 SpendingPolicy + AgentIdentity + PolicyEnforcer","text":"<p>Status: Draft Author: Dhruv Sanan Related issue: solvapay/solvapay-sdk#187</p>"},{"location":"rfcs/0001-spending-policy/#problem","title":"Problem","text":"<p>In multi-agent pipelines, the same customer reference is shared across many tool calls. Today there is no way for the SDK to enforce per-agent or per-session spending limits without rolling custom middleware.</p>"},{"location":"rfcs/0001-spending-policy/#proposal","title":"Proposal","text":""},{"location":"rfcs/0001-spending-policy/#agentidentity","title":"<code>AgentIdentity</code>","text":"<pre><code>@dataclass(frozen=True)\nclass AgentIdentity:\n    agent_id: str\n    session_id: str | None = None\n    model: str | None = None\n    parent_agent_id: str | None = None\n</code></pre>"},{"location":"rfcs/0001-spending-policy/#spendingpolicy","title":"<code>SpendingPolicy</code>","text":"<pre><code>@dataclass(frozen=True)\nclass SpendingPolicy:\n    max_calls_per_session: int | None = None\n    max_spend_usd: float | None = None\n    allowed_products: frozenset[str] = field(default_factory=frozenset)\n</code></pre>"},{"location":"rfcs/0001-spending-policy/#policyenforcer-protocol-already-in-solvapaypaywallpolicy","title":"<code>PolicyEnforcer</code> Protocol (already in <code>solvapay.paywall.policy</code>)","text":"<pre><code>class PolicyEnforcer(Protocol):\n    def check(self, customer_ref: str, product: str, **context: Any) -&gt; bool: ...\n</code></pre>"},{"location":"rfcs/0001-spending-policy/#design-notes","title":"Design notes","text":"<ul> <li><code>PolicyEnforcer</code> lives in <code>solvapay.experimental</code> until the shape stabilizes.</li> <li><code>Paywall</code> accepts an optional <code>enforcer: PolicyEnforcer | None</code> kwarg. When set,   the enforcer is called BEFORE the <code>check_limits</code> API call, enabling fast local   rejection without a network round-trip.</li> <li><code>AgentIdentity</code> is passed via <code>Context.extras[\"agent_identity\"]</code> in <code>RequestSpec</code>.</li> </ul>"},{"location":"rfcs/0001-spending-policy/#open-questions","title":"Open questions","text":"<ol> <li>Should <code>SpendingPolicy</code> be server-side (SolvaPay API) or client-side (SDK)?</li> <li>How do we handle session TTL \u2014 SDK tracks it or SolvaPay backend?</li> <li><code>parent_agent_id</code> for hierarchical budgeting \u2014 necessary in v1?</li> </ol>"},{"location":"rfcs/0001-spending-policy/#next-steps","title":"Next steps","text":"<p>Open GitHub Discussion linking this RFC for community input before implementation. Ship <code>PolicyEnforcer</code> stub is already in <code>solvapay.paywall.policy</code> (v0.8).</p>"},{"location":"rfcs/0002-openapi-investigation/","title":"RFC 0002 \u2014 OpenAPI Spec Investigation","text":"<p>Status: Open Date: 2026-05-23 Author: Dhruv Sanan HLD reference: \u00a7V1.9</p>"},{"location":"rfcs/0002-openapi-investigation/#question","title":"Question","text":"<p>Does SolvaPay publish an OpenAPI / Swagger spec for the server API? If yes, it activates the <code>_generated/</code> model slot in HLD \u00a7V1.9 for V3.</p>"},{"location":"rfcs/0002-openapi-investigation/#action-items","title":"Action items","text":"<ul> <li>[ ] File GitHub issue against <code>solvapay/solvapay-sdk</code> asking whether an OpenAPI spec is published</li> <li>[ ] Check standard paths: <code>https://api.solvapay.com/openapi.json</code>, <code>https://api.solvapay.com/v1/openapi.json</code></li> <li>[ ] Check SolvaPay developer docs for spec download link</li> </ul>"},{"location":"rfcs/0002-openapi-investigation/#current-approach-no-spec","title":"Current approach (no spec)","text":"<p>Without an OpenAPI spec, models are hand-written in <code>src/solvapay/models.py</code> and validated against the real sandbox API. The <code>_generated/</code> slot in <code>src/solvapay/</code> is reserved but empty (HLD \u00a7V1.9).</p>"},{"location":"rfcs/0002-openapi-investigation/#if-spec-is-found","title":"If spec is found","text":"<ol> <li>Compare spec endpoints against <code>src/solvapay/operations/_registry.py</code> \u2014 identify gaps</li> <li>Evaluate <code>openapi-python-client</code> or <code>datamodel-code-generator</code> for model generation</li> <li>Plan V3 <code>_generated/</code> activation \u2014 coordinate with HLD \u00a7V1.9 gating criteria</li> </ol>"},{"location":"rfcs/0002-openapi-investigation/#if-spec-is-not-available","title":"If spec is not available","text":"<p>Leave <code>_generated/</code> slot reserved. Continue manual model maintenance. Re-evaluate at V3 planning.</p>"},{"location":"rfcs/0002-openapi-investigation/#findings","title":"Findings","text":"<p>TBD \u2014 pending backend response to GitHub issue.</p>"}]}